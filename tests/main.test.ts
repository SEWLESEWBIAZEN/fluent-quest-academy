import { it, expect, describe } from 'vitest'

describe('Main', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })
})

describe('group', () => {
  it('should work', () => {
    expect(1).toBeTruthy()
  })
})